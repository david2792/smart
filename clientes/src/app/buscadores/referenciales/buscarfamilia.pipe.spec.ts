import { BuscarfamiliaPipe } from './buscarfamilia.pipe';

describe('BuscarfamiliaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarfamiliaPipe();
    expect(pipe).toBeTruthy();
  });
});
