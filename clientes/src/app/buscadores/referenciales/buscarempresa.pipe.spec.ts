import { BuscarempresaPipe } from './buscarempresa.pipe';

describe('BuscarempresaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarempresaPipe();
    expect(pipe).toBeTruthy();
  });
});
