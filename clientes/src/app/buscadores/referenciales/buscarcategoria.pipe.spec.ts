import { BuscarcategoriaPipe } from './buscarcategoria.pipe';

describe('BuscarcategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarcategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
