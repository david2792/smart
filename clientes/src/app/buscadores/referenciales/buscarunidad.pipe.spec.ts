import { BuscarunidadPipe } from './buscarunidad.pipe';

describe('BuscarunidadPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarunidadPipe();
    expect(pipe).toBeTruthy();
  });
});
