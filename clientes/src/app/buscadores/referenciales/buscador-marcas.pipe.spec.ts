import { BuscadorMarcasPipe } from './buscador-marcas.pipe';

describe('BuscadorMarcasPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorMarcasPipe();
    expect(pipe).toBeTruthy();
  });
});
