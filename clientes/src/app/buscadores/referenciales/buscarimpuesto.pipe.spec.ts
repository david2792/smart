import { BuscarimpuestoPipe } from './buscarimpuesto.pipe';

describe('BuscarimpuestoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarimpuestoPipe();
    expect(pipe).toBeTruthy();
  });
});
