import { BuscarpresentacionPipe } from './buscarpresentacion.pipe';

describe('BuscarpresentacionPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarpresentacionPipe();
    expect(pipe).toBeTruthy();
  });
});
